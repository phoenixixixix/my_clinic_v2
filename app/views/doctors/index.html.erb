<h1 class="font-bold text-5xl mb-16">Doctors List</h1>

<div class="overflow-x-auto">
  <table class="table">
    <thead>
    <tr>
      <th>Full Name</th>
      <th>Appointments №</th>
      <th></th>
    </tr>
    </thead>
    <tbody>

    <% @doctors.each do |doctor| %>
      <tr>
        <td><%= doctor.full_name %></td>
        <td><%= doctor.appointments.size %></td>
        <td><%= link_to "book",
                        appointments_path(appointment: { doctor_id: doctor.id } ),
                        data: { turbo_method: :post },
                        class: "btn btn-accent btn-sm" %>
        </td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
